DISCORD_TOKEN=your-bot-token-here
# Optional: set to your test server's ID for instant slash-command sync during
# development. Leave blank for global sync (can take up to ~an hour to appear).
GUILD_ID=
